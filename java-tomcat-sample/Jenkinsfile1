pipeline{
	agent any
	stages{
		stage('Build Application'){
		 steps{
			sh 'mvn -f clean package'
		 }
		post {
		 success{
		 echo 'Now archieving the artifacts...'
		 archiveArtifacts artifacts: '**/*.war'
		 }
		}
		}
		
	}
}
